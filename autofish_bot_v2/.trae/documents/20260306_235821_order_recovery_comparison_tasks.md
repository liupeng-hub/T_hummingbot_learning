# 订单恢复逻辑对比任务清单

## 任务列表

### 1. 修复 base_price 更新问题
- [ ] 在 `_restore_orders` 方法中添加 `self.chain_state.base_price = current_price`

### 2. 验证订单状态设置方式
- [ ] 检查 `Order` 类是否有 `set_state` 方法
- [ ] 如果有，统一使用 `set_state` 方法设置状态

### 3. 测试验证
- [ ] 运行单元测试确保修改不影响现有功能
- [ ] 手动测试订单恢复流程

## 优先级

| 任务 | 优先级 | 风险 |
|------|--------|------|
| base_price 更新 | 高 | 低 |
| 订单状态设置 | 中 | 低 |
| 测试验证 | 高 | 无 |
