# 移除行情数据存储 - 规格

## 变更内容

### 数据库表结构变更
1. 删除 `market_statuses` 表
2. 删除 `Market_status_changes` 字段
3. 删除 `trading_statuses` 字段
4. 删除 `get_history_by_algorithm` 方法，    - 修改 `get_history_by_algorithm` 方法返回 `by_algorithm` 列表

    - 修改 `get_history_by_symbol` 方法返回 `by_symbol` 列表
    - 修改 `get_history_by_type` 方法返回 `by_type` 列表
    - 修改 `get_history_by_market_algorithm` 方法返回 `by_market_algorithm` 列表
    - 修改 `get_history_by_date_range` 方法返回 `by_date_range`
    - 修改 `get_history_summary` 方法返回 `summary` dict
5. 删除 `market_status_changes` 字段
    - 删除 `market_status_changes` 字段 (保留状态变化次数)
    - 删除 `market_status_changes` 字段 (保留状态变化次数)
    - 删除 `market_status_changes` 字段 (保留状态变化次数)
    - 删除 `market_status_changes` 字段 (保留状态变化次数)
    - 删除 `market_status_changes` 字段 (保留状态变化原因)
        - 保留 `upper_band`、 `lower_band` 字段
    - 删除 `upper_band` 和 `lower_band` 字段
    - 删除 `extra_data` 字段
    - 删除 `extra_data` 字段
    - 删除 `extra_metrics` 字段
    - 删除 `market_status_changes` 字段
    - 删除 `market_status_changes` 字段 (保留状态变化次数)
        - 保留 `market_status_changes` 字段 (保留状态变化原因)
        . 保留 `market_status_changes` 字段 (保留状态变化原因)
        . 保留 `market_status_changes` 字段 (保留状态变化原因)
        . 保留 `market_status_changes` 字段 (保留状态变化原因)
        . 保留 `market_status_changes` 字段 (保留状态变化原因)
        . 保留 `market_status_changes` 字段 (保留状态变化原因)
        - 保留 `market_status` 字段 (保留状态变化次数)
        - 保留 `market_status` 字段 (保留状态变化次数)
    - 删除 `market_status_changes` 字段
    - 删除 `market_status_changes` 字段 (保留状态变化次数)
        - 保留 `market_status_changes` 字段 (保留状态变化原因)
        - 保留 `market_status` 字段 (保留状态变化原因)
        - 保留 `market_status` 字段 (保留状态变化原因)
        - 保留 `market_status` 字段 (保留状态变化原因)
        - 保留 `market_status` 字段 (保留状态变化原因)
        - 保留 `market_status` 字段 (保留状态变化原因)
            - 保留 `market_status` 字段 (保留状态变化次数)
            - 保留 `market_status` 字段 (保留状态变化原因)
                - - 保留 `market_status` 字段 (保留状态变化原因)
                    -- 保留 `market_status` 字段 (保留状态变化原因)
                }
            }
        }
    }
    // 删除 get_chart_data API
        - 删除 get_chart_data(result_id) 方法
        - 删除 get_chart_data API
            - 删除 get_chart_data API 硌 floor
            - 删除 get_chart_data API
                - 删除 get_chart_data API
                    // 删除 get_chart_data API
                    - 删除 get_chart_data API
                    // 删除 get_chart_data API
                    - 删除 get_chart_data API
                        - 删除 get_chart_data API
                        - 删除 get_chart_data API
                            - 删除 get_chart_data API
                            - 删除 get_chart_data API
                            - 删除 get_chart_data API
                            - 刂删除 get_chart_data API
                        - 刂 删除 get_chart_data API
                            - 刂删除 get_chart_data API
                            - 别删除 get_chart_data API
                        - 刂删除 get_chart_data API
                            - 别删除 get_chart_data API
                            - 别删除 get_chart_data API
                            - 刂删除 get_chart_data API
                            - 刂删除 get_chart_data API
                        - 刍 删除 get_chart_data API
                            - 刂删除 get_chart_data API
                            - 刍删除 get_chart_data API
                            - 刂删除 get_chart_data API
                        - 刍删除 get_chart_data API
                            - 别 delete get_chart_data API
                        - 刍删除 get_chart_data API
                            - 刍删除 get_chart_data API
                            - 刍删除 get_chart_data API
                            - 刍删除 get_chart_data API
                            - 刍删除 get_chart_data API
                            - lchart modal
                            - lchart modal 弹窗
                            . lchart.modal-header
                                .lchart.modal-body
                                    .div id="klineChart"></div>
                        </div>
                    </div>
                </div>
            }
        });
    }
</script>
</body>
</html>
``` </div class="modal fade" id="chartModal" tabindex="-1" aria-hidden="true">
                </div>
        </div>
        $('#klineChart'). {
            display: 'none';
        }
    </ </script>
    </ if (showMarketStatus) {
                klineChart.markArea.data = marketStatus_background;
            }
        }
    }
    </ else {
                color: 'rgba(239, 68, 0.3,)';
            itemStyle['z-index': 11, } else {
                }
            }
        }
    }
    </style>
        }
        </`#chartModal .modal-header . #chartModal .modal-footer {
                <button type="button" class="btn btn-outline-primary" onclick="showChart">>查看K线图">详情</button>
                        <button class="btn btn-outline-info" onclick="showChart" >查看详情)
                        <button class="btn btn-outline-success" onclick="showChart") >查看对比结果)
                    }
                }
            });
        }
    </script>
</script>
</body>
</html>
                    </tbody.appendChild(row.innerHTML = `
                        <td>${r.status} === 'completed') {
                            <td class="status-badge status-${status}">${td}</td}
                            <td class="status-badge status-${status}">${td}</td}
                            <td class="status-badge status-${status}">{td}</td}
                            <td class="status-badge.status-${status}">{td}</td}
                            <td class="status-badge.status-${status}">{td}</td}
                            <td class="status-badge.status-${status}">{td}</11}
                            <td class="status-badge.status-${status}">{td}.12}
                            <td class="status-badge.status-${status}">{td}.13}
                            <td class="status-badge.status-${status}">{td}.14}
                            <td class="status-badge.status-${status}">{td}.15}
                            <td class="status-badge.status-${status}">{td}.16}
                            <td class="status-badge.status-${status}">{td}.17}
                            <td class="status-badge.status-${status}">{td}.18}
                            <td class="status-badge.status-${status}">{td}.19}
                            <td class="status-badge.status-${status}">{td}.20}
                            <td class="status-badge.status-${status}">{td}.21}
                        </td>
                    </tbody>
                </table>
            </ </script>
</body>
</html>