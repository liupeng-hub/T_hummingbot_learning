# 回测自动补充缺失 K 线数据 - 任务列表

## 任务概览

| # | 任务 | 优先级 | 状态 |
|---|------|--------|------|
| 1 | 创建缓存检查模块 | 高 | 待开始 |
| 2 | 修改回测程序集成自动获取 | 高 | 待开始 |
| 3 | 添加命令行参数 | 中 | 待开始 |
| 4 | 测试验证 | 高 | 待开始 |

## 详细任务

### 任务 1：创建缓存检查模块

**文件**：`binance_kline_fetcher.py`

**修改内容**：
- 在 `KlineFetcher` 类中添加 `check_coverage()` 方法
- 添加 `find_missing_ranges()` 方法
- 返回缺失的时间范围

**验收标准**：
- 能正确检测缓存覆盖情况
- 能返回缺失的时间范围列表

---

### 任务 2：修改回测程序集成自动获取

**文件**：`binance_backtest.py`

**修改内容**：
- 修改 `fetch_klines()` 方法
- 添加缓存检查逻辑
- 自动调用 `KlineFetcher` 补充缺失数据
- 显示进度和状态信息

**验收标准**：
- 缓存完整时直接读取
- 缓存缺失时自动补充
- 失败时提示手动命令

---

### 任务 3：添加命令行参数

**文件**：`binance_backtest.py`

**修改内容**：
- 添加 `--no-auto-fetch` 参数
- 默认启用自动获取

**验收标准**：
- 默认行为：自动补充缺失数据
- `--no-auto-fetch`：仅使用缓存

---

### 任务 4：测试验证

**测试场景**：
1. 缓存完整 - 直接读取
2. 缓存缺失 - 自动补充
3. 缓存为空 - 自动获取全部
4. 禁用自动获取 - 提示手动操作

**验收标准**：
- 所有场景按预期工作
- 无报错
