# 回测自动补充缺失 K 线数据 - 检查清单

## 开发阶段

### 任务 1：创建缓存检查模块

- [ ] 在 `binance_kline_fetcher.py` 中添加 `find_missing_ranges` 方法
  - [ ] 方法签名正确
  - [ ] 返回缺失时间范围列表
  - [ ] 处理缓存为空的情况

### 任务 2：修改回测程序集成自动获取

- [ ] 修改 `binance_backtest.py` 的 `fetch_klines` 方法
  - [ ] 导入 `KlineFetcher` 类
  - [ ] 调用 `find_missing_ranges` 检查缺失
  - [ ] 缺失时调用 `_fetch_from_api` 获取数据
  - [ ] 获取成功后保存到缓存
  - [ ] 显示缺失信息提示
  - [ ] 显示自动获取进度

### 任务 3：添加命令行参数

- [ ] 添加 `--no-auto-fetch` 参数
  - [ ] 参数默认值为 `False`（启用自动获取）
  - [ ] 参数传递到 `fetch_klines` 方法

### 任务 4：测试验证

- [ ] 测试缓存完整场景
  - [ ] 运行 `--days 5`（缓存有 10 天数据）
  - [ ] 验证不调用 API
  - [ ] 验证直接从缓存读取

- [ ] 测试缓存缺失场景
  - [ ] 运行 `--days 20`（缓存只有 15 天数据）
  - [ ] 验证显示缺失提示
  - [ ] 验证自动获取缺失数据
  - [ ] 验证回测正常进行

- [ ] 测试缓存为空场景
  - [ ] 清空缓存
  - [ ] 运行 `--days 10`
  - [ ] 验证自动获取全部数据
  - [ ] 验证回测正常进行

- [ ] 测试禁用自动获取场景
  - [ ] 运行 `--days 10 --no-auto-fetch`（缓存不完整）
  - [ ] 验证显示手动操作提示
  - [ ] 验证不调用 API

## 验收标准

### 功能验收

- [ ] 缓存完整时直接使用缓存
- [ ] 缓存缺失时自动补充
- [ ] 显示缺失信息提示
- [ ] 显示获取进度
- [ ] 获取失败时提示手动操作
- [ ] `--no-auto-fetch` 参数正常工作

### 代码质量

- [ ] 无语法错误
- [ ] 无运行时错误
- [ ] 日志输出清晰
- [ ] 代码注释完整

### 用户体验

- [ ] 提示信息清晰易懂
- [ ] 进度显示准确
- [ ] 错误提示有帮助

## 完成标志

- [ ] 所有测试用例通过
- [ ] 代码已提交
- [ ] 文档已更新
